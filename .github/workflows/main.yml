name: Daily News Email

on:
  schedule:
    - cron: '0 23 * * *'
  workflow_dispatch:

jobs:
  collect-and-send:
    runs-on: ubuntu-latest
    steps:
      - name: Collect news and send email
        run: |
          node << 'EOF'
          const ANTHROPIC_API_KEY = process.env.ANTHROPIC_API_KEY;
          const SUPABASE_URL = process.env.SUPABASE_URL;
          const SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY;
          const BASE_URL = 'https://news-digest-tau-umber.vercel.app';

          async function fetchJSON(url, options) {
            const res = await fetch(url, options);
            return res.json();
          }

          async function collectNews(sectionLabel, isWar) {
            const warPrompt = '오늘 날짜 기준 이란, 미국, 이스라엘 간 전쟁 및 분쟁의 최신 현황을 웹에서 검색하여 가장 중요한 3가지를 아래 형식으로 답하세요. 분석내용에는 반드시 군사적현황, 정치적파급효과, 글로벌경제및에너지영향, 한국경제영향을 각각 2-3문장씩 포함하여 상세히 작성해주세요:\n##1##제목||[군사적현황] 내용. [정치적파급] 내용. [경제영향] 내용. [한국경제] 내용.\n##2##제목||[군사적현황] 내용. [정치적파급] 내용. [경제영향] 내용. [한국경제] 내용.\n##3##제목||[군사적현황] 내용. [정치적파급] 내용. [경제영향] 내용. [한국경제] 내용.';
            const generalPrompt = '오늘 날짜 기준 ' + sectionLabel + ' 분야에서 세계에 영향을 미칠 수 있는 최신
